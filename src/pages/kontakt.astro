---
import Layout from '../layouts/Layout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
---

<Layout 
  title="Kontakt - Toma Džusy"
  description="Kontaktujte nás a sdělte nám své příběhy s našimi výrobky."
>
  <Navigation />
  
  <main class="pt-20">
    <!-- Hero Section -->
    <section class="py-16 bg-gradient-to-br from-orange-50 to-yellow-50">
      <div class="container-custom">
        <div class="max-w-3xl mx-auto text-center space-y-6">
          <h1 class="text-5xl lg:text-6xl font-bold text-gray-900">
            Kontaktujte nás
          </h1>
          <p class="text-lg text-gray-700">
            Toma je pro nás víc než jen džus. Je to radost, kterou chceme sdílet s vámi. 
            Sledujte nás na sociálních sítích a podělte se o své chvíle se značkou Toma. 
            Máte fotku nebo vzkaz? Kontaktujte nás! Jsme tu pro vás a vaše příběhy s našimi 
            výrobky nás baví. Těšíme se na každý sdílený okamžik!
          </p>
        </div>
      </div>
    </section>

    <!-- Contact Form Section -->
    <section class="py-16">
      <div class="container-custom">
        <div class="max-w-2xl mx-auto">
          <form 
            name="kontakt"
            method="POST"
            data-netlify="true"
            netlify-honeypot="bot-field"
            class="space-y-6 bg-white rounded-2xl shadow-xl p-8"
          >
            <input type="hidden" name="form-name" value="kontakt" />
            <p class="hidden">
              <label>
                Don't fill this out if you're human: <input name="bot-field" />
              </label>
            </p>

            <div>
              <label for="jmeno" class="block text-sm font-medium text-gray-700 mb-2">
                Jméno
              </label>
              <input
                type="text"
                id="jmeno"
                name="jmeno"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-toma-orange focus:border-transparent transition-all"
                placeholder="Vaše jméno"
              />
            </div>

            <div>
              <label for="prijmeni" class="block text-sm font-medium text-gray-700 mb-2">
                Příjmení
              </label>
              <input
                type="text"
                id="prijmeni"
                name="prijmeni"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-toma-orange focus:border-transparent transition-all"
                placeholder="Vaše příjmení"
              />
            </div>

            <div>
              <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
                Email
              </label>
              <input
                type="email"
                id="email"
                name="email"
                required
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-toma-orange focus:border-transparent transition-all"
                placeholder="vas@email.cz"
              />
            </div>

            <div>
              <label for="zprava" class="block text-sm font-medium text-gray-700 mb-2">
                Vaše zpráva
              </label>
              <textarea
                id="zprava"
                name="zprava"
                required
                rows="6"
                class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-toma-orange focus:border-transparent transition-all resize-none"
                placeholder="Napište nám svůj příběh s Toma džusy..."
              ></textarea>
            </div>

            <button
              type="submit"
              class="w-full px-8 py-4 bg-toma-orange text-white font-semibold rounded-lg hover:bg-orange-600 transition-all hover:scale-[1.02] shadow-lg"
            >
              Odeslat zprávu
            </button>
          </form>
        </div>
      </div>
    </section>

    <!-- Image Gallery Section -->
    <section class="py-16 bg-gray-50">
      <div class="container-custom">
        <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
          {[1, 2, 3, 4].map((num) => (
            <div class="aspect-square overflow-hidden rounded-xl">
              <img
                src={`/images/kontakt-${num}.png`}
                alt={`Toma džusy ${num}`}
                class="w-full h-full object-cover hover:scale-110 transition-transform duration-300"
              />
            </div>
          ))}
        </div>
      </div>
    </section>
  </main>

  <Footer />
</Layout>

